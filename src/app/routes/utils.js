

export const requireIntro = (nextState, replace) => {
  /* //TODO can't use because no access to store
  switch(store.getState().user.stage){
    case 0:

      replace({
        pathname: '/intro',
        state: { nextPathname: nextState.location.pathname }
      });
      break;
    case 1:
      replace({
        pathname: '/splash',
        state: { nextPathname: nextState.location.pathname }
      });
      break;
  } */
}